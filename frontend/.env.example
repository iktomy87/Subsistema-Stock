# ===========================================
# VARIABLES DE ENTORNO PARA DEPLOYMENT
# ===========================================

# ---- PostgreSQL (solo para desarrollo local con Docker) ----
POSTGRES_DB=keycloak_db
POSTGRES_USER=keycloak_db_user
POSTGRES_PASSWORD=keycloak_db_user_password

# ---- Keycloak Admin (solo para desarrollo local) ----
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=ds2025

# ---- NextAuth.js Configuration ----
# Secret key para NextAuth.js - Genera uno nuevo con: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here

# URL de tu aplicación (cambiar según el entorno)
# Desarrollo: http://localhost:3000 o http://localhost:8080
# Producción: https://tu-app.vercel.app
NEXTAUTH_URL=http://localhost:8080

# ---- Keycloak OAuth Configuration ----
# Client ID del cliente OAuth configurado en Keycloak
KEYCLOAK_CLIENT_ID=grupo-08

# Client Secret del cliente OAuth (obtener desde Keycloak Admin)
KEYCLOAK_CLIENT_SECRET=your-client-secret-here

# URL del Issuer de Keycloak (Realm URL)
KEYCLOAK_ISSUER=https://keycloak.cubells.com.ar/realms/ds-2025-realm

# URL del endpoint Well-Known de OpenID Connect
KEYCLOAK_WELL_KNOWN_URL=https://keycloak.cubells.com.ar/realms/ds-2025-realm/.well-known/openid-configuration

# ---- API Backend Configuration ----
# URL del backend API (opcional - se usa en next.config.ts rewrites)
NEXT_PUBLIC_STOCK_API_URL=https://api.cubells.com.ar/stock
